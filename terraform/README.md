# Flask + Express on Single EC2 via Terraform

## Deployment Instructions

1. Update `variables.tf` with appropriate values.
2. Run:
   ```bash
   terraform init
   terraform plan
   terraform apply
