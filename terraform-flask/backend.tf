# backend.tf
terraform {
  # backend "s3" {
  #   bucket         = "your-s3-bucket-name"
  #   key            = "terraform.tfstate"
  #   region         = "ap-south-1"
  #   dynamodb_table = "terraform-lock"
  # }
}
